services:
  - type: web
    name: your-app
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gfortran
      pip install -r requirements.txt
    startCommand: python app.py